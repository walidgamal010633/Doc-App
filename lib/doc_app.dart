import 'package:flutter/material.dart';

class Doc_App extends StatelessWidget {
  const Doc_App({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}